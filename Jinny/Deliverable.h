#pragma once

namespace  jinny
{
    /**
     * \brief Base class for all things sent and received by classes
     */
    struct Deliverable
    {
        // So far doesn't have anything else special but mainly used to distinguish between events and messages
    };
}
